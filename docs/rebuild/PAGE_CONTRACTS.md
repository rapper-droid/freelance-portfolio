# PAGE_CONTRACTS — 画面ごとの契約

生成: `node scripts/page-contracts.mjs`。route と実体数は
[ROUTE_INVENTORY.json](ROUTE_INVENTORY.json) から結合しているため、
実際に配信されている画面とずれない。テンプレートが増えて契約がなければ失敗する。

**35 テンプレート / 95 実体。** 状態契約は 23 項目で、該当しないものは理由つきで N/A とする。

## 一覧

| テンプレート | brand | 実体 | 利用者 | 目的 | 該当する状態 |
| --- | --- | ---: | --- | --- | ---: |
| `/` | TSUDOWA | 1 | TSUDOWA を初めて見る人、どの窓口か探している人 | 親ブランドの構造を理解し、制作（WORKS）か実験（LAB）へ進む | 4/23 |
| `/automation` | TETSU WORKS | 1 | 実行モデルが妥当かを判断する人（開発者・情報システム） | RELAY の 1 回の実行を、業務の言葉ではなく契約の粒度で確認する | 19/23 |
| `/contact` | TETSU WORKS | 1 | 制作を相談したい人 | 相談内容を送り、受付番号を受け取る | 20/23 |
| `/contact/general` | TSUDOWA | 1 | 制作以外の用件がある人 | TSUDOWA 全体への問い合わせを送る | 20/23 |
| `/demos/[slug]` | TETSU WORKS | 11 | 動かして確かめたい人 | 制作例を実際に操作する | 19/23 |
| `/flow` | TETSU WORKS | 1 | 自動化で何が減るのか知りたい人 | 受付→整理→出力までを実際に流す | 19/23 |
| `/history` | TSUDOWA | 1 | 実際に何が作られたか確かめたい人 | 年ごとの制作事実を、コミット参照つきで読む | 4/23 |
| `/lab` | TSUKUTTA LAB | 1 | プロダクト・実験に関心がある人 | LAB が独立した活動であることを知る | 4/23 |
| `/partners` | TETSU WORKS | 1 | 制作会社・デザイナー（実装の外注先を探す側） | 実装品質を確かめ、条件を整理して相談する | 20/23 |
| `/privacy` | TSUDOWA | 1 | 問い合わせ前に取り扱いを確認したい人 | 収集する項目・保管・第三者提供を確認する | 4/23 |
| `/projects/[slug]` | TETSU WORKS | 11 | 特定の制作例を詳しく見たい人 | 背景・課題・実装・納品物・QA・限界を読む | 4/23 |
| `/report` | TETSU WORKS | 1 | 毎週・毎月、同じ形式のCSVを集計している人 | 列の意味を一度決めて保存し、二回目からは質問なしで集計する | 20/23 |
| `/rescue` | TETSU WORKS | 1 | 何を頼めばよいか分からない人 | 困りごとから相談文を組み立てる | 20/23 |
| `/services` | TETSU WORKS | 1 | 小さく頼める範囲を探している人 | 範囲・価格・期間が決まったメニューを選ぶ | 13/23 |
| `/services/[slug]` | TETSU WORKS | 2 | そのメニューを頼むか決める人 | 範囲・進め方・検証済みの内容を読み、相談文を作る | 20/23 |
| `/works` | TETSU WORKS | 1 | 依頼を検討している人 | 自分の仕事に近い制作例と参考価格を見つける | 13/23 |
| `/works/[category]` | TETSU WORKS | 12 | 依頼の種類が決まっている人 | その分類の制作例・範囲・必要な素材・参考価格を読む | 13/23 |
| `/experience/[slug]` | TETSU WORKS | 11 | デモだけを画面いっぱいで見たい人 | ポートフォリオの枠なしで同じデモを操作する | 19/23 |
| `/report/recipes` | TETSU WORKS | 1 | 保存したルールを確かめたい人 | 列の対応・通貨・重複条件・承認済みの税区分と版を読む | 13/23 |
| `/report/history` | TETSU WORKS | 1 | 先週の数字を確かめたい人 | 過去の処理の合計・件数・例外の数と、使ったルールの版を読む | 13/23 |
| `/daybook` | TETSU WORKS | 1 | 予約の希望を文章で送りたい人 | 希望の時間が取れるか判定し、取れないときは実際に空いている候補まで出す | 20/23 |
| `/daybook/admin` | TETSU WORKS | 1 | 予約を受ける側 | 承認が要るものと、承認すると何が変わるかを読んで決める | 20/23 |
| `/kissa` | KISSA（架空店舗） | 1 | 店を探している人（の役） | 店の雰囲気を見て、注文か予約へ進む | 20/23 |
| `/kissa/menu` | KISSA（架空店舗） | 1 | 注文する人 | 14 品から選び、カートに入れる | 13/23 |
| `/kissa/reserve` | KISSA（架空店舗） | 1 | 席を取る人 | 空席から選び、仮押さえして確定する | 20/23 |
| `/kissa/order` | KISSA（架空店舗） | 1 | 注文を確定する人 | 受取時間を決めて注文する | 20/23 |
| `/kissa/my` | KISSA（架空店舗） | 1 | 注文・予約した人 | 状況を見て、変更・取消し、データを持ち出す | 20/23 |
| `/kissa/admin` | KISSA（架空店舗） | 1 | 店側（の役） | 注文を進め、売り切れを切り替え、売上を見る | 20/23 |
| `/kissa/menu/[productId]` | KISSA（架空店舗） | 14 | その商品を選ぶ人 | 温度・サイズを選び、数量を決めてカートに入れる | 20/23 |
| `/forme` | FORME（架空店舗） | 1 | 商品を探している人（の役） | 店の世界観を見て、商品一覧へ進む | 20/23 |
| `/forme/items` | FORME（架空店舗） | 1 | 商品を比べる人 | 絞り込み・並び替え・3 点比較で候補を絞る | 13/23 |
| `/forme/cart` | FORME（架空店舗） | 1 | 注文する人 | 受け取り方法と地域を決め、送料込みで注文する | 20/23 |
| `/forme/my` | FORME（架空店舗） | 1 | 注文した人 | 状況を見て、支払い・取消しをし、データを持ち出す | 20/23 |
| `/forme/admin` | FORME（架空店舗） | 1 | 店側（の役） | 在庫を動かし、取り扱いを止め、発送し、売上を見る | 20/23 |
| `/forme/items/[productId]` | FORME（架空店舗） | 6 | その商品を選ぶ人 | 色とサイズを選び、価格と在庫を確かめて入れる | 20/23 |

## 契約

### `/`

TSUDOWA / static / 実体 1 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | TSUDOWA を初めて見る人、どの窓口か探している人 |
| 目的 | 親ブランドの構造を理解し、制作（WORKS）か実験（LAB）へ進む |
| 入力 | なし（読むだけ） |
| 出力 | なし |
| 次の操作 | /works・/lab・/contact・/history |
| 見せ場 | 二つの世界と、日付つきの制作記録 |
| 減らす手間 | 「何をしている人か」を問い合わせで聞く手間 |
| 画像 | KISSA の店舗写真、各デモの実画面プレビュー |
| 保存 | なし |
| 副作用・権限 | PostHog の portfolio_visit（設定時のみ） |
| 完了判定と証拠 | hq.spec.ts / visual-qa 4 幅 / qa:performance |

状態: 4/23 該当。N/A — **入力中**（入力欄がない）、**選択**（選択状態を持つ操作がない）、**loading**（サーバー取得なし（静的生成））、**進行**（複数段階の処理がない）、**成功**（完了する操作がない）、**空**（内容は静的で、空になりえない）、**検索0件**（検索がない）、**validation error**（入力がない）、**通信失敗**（リクエストを出さない）、**offline**（初回表示後は追加取得がない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）、**競合**（他者と共有する状態がない）、**部分成功**（分割される処理がない）、**retry**（失敗しうる処理がない）、**cancel**（取り消す操作がない）、**undo**（変更が残らない）、**disabled**（無効化する操作がない）、**再訪**（保存しないため初回と同じ）

### `/automation`

TETSU WORKS / demo / 実体 1 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 実行モデルが妥当かを判断する人（開発者・情報システム） |
| 目的 | RELAY の 1 回の実行を、業務の言葉ではなく契約の粒度で確認する |
| 入力 | 同梱サンプルの選択、承認する操作の選択、相手先の挙動（成功・失敗・応答なし） |
| 出力 | 実行計画、承認の紐づけ、操作ごとの結果、実行ログ（JSON） |
| 次の操作 | /demos/automation（業務側）・/demos/inbox（運用側） |
| 見せ場 | 失敗と「結果が分からない」を分け、再実行で二重に実行しないことを実際に見せる |
| 減らす手間 | 「本当に安全に再実行できるのか」を問い合わせて確認する手間 |
| 画像 | なし（識別子と状態が主役） |
| 保存 | なし（この画面は何も保存しない） |
| 副作用・権限 | なし。実行は端末内の模擬アダプター。任意コード実行・任意URL取得なし |
| 完了判定と証拠 | automation-console.test.ts（17 件）/ automation.spec.ts（8 件） |

状態: 19/23 該当。N/A — **通信失敗**（サーバーへ出ない）、**offline**（サーバーへ出ない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）

### `/contact`

TETSU WORKS / form / 実体 1 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 制作を相談したい人 |
| 目的 | 相談内容を送り、受付番号を受け取る |
| 入力 | 種別・内容・連絡先（必須は最小限） |
| 出力 | 受付番号、確認メール |
| 次の操作 | 完了表示 |
| 見せ場 | 曖昧な段階でも送れる。送信前に内容を確認できる |
| 減らす手間 | 問い合わせ文を一から書く手間 |
| 画像 | なし |
| 保存 | Durable Object（CONTACT_STATE）。デモのデータは入らない |
| 副作用・権限 | 実メール送信。portfolio_contact_submit / _success |
| 完了判定と証拠 | contact.spec.ts / contact のユニットテスト群 / abuse.test.ts |

状態: 20/23 該当。N/A — **権限不足**（アカウントの概念がない）、**競合**（同じ相談を二人が編集しない）、**undo**（送信前は編集、送信後は取り消せないと明記）

### `/contact/general`

TSUDOWA / form / 実体 1 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 制作以外の用件がある人 |
| 目的 | TSUDOWA 全体への問い合わせを送る |
| 入力 | 内容・連絡先 |
| 出力 | 受付番号、確認メール |
| 次の操作 | 完了表示・/contact（制作の相談） |
| 見せ場 | 制作窓口と混ざらないよう入口を分ける |
| 減らす手間 | 宛先を迷う手間 |
| 画像 | なし |
| 保存 | CONTACT_STATE |
| 副作用・権限 | 実メール送信 |
| 完了判定と証拠 | contact.spec.ts / hq.spec.ts（導線） |

状態: 20/23 該当。N/A — **権限不足**（アカウントの概念がない）、**競合**（同じ相談を二人が編集しない）、**undo**（送信前は編集、送信後は取り消せないと明記）

### `/demos/[slug]`

TETSU WORKS / demo / 実体 11 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 動かして確かめたい人 |
| 目的 | 制作例を実際に操作する |
| 入力 | デモごと（選択・入力・チェック） |
| 出力 | デモごと（書き出し・記録・表示） |
| 次の操作 | /experience/<slug>・操作できる版・/contact |
| 見せ場 | 見せかけを置かない。固定表示は固定表示と書く |
| 減らす手間 | 打ち合わせでの画面説明 |
| 画像 | デモ内の作図・実画面 |
| 保存 | RELAY / INBOX / ADMIN のみ ops sandbox |
| 副作用・権限 | portfolio_live_demo_click |
| 完了判定と証拠 | portfolio.spec.ts / ops.spec.ts / showcase-p3.spec.ts / qa:controls |

状態: 19/23 該当。N/A — **通信失敗**（サーバーへ出ない）、**offline**（サーバーへ出ない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）

実体: `/demos/admin` / `/demos/automation` / `/demos/booking` / `/demos/cafe` / `/demos/creative` / `/demos/csv` / `/demos/ec` / `/demos/improvement` / `/demos/inbox` / `/demos/qa` / `/demos/saas`

### `/flow`

TETSU WORKS / demo / 実体 1 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 自動化で何が減るのか知りたい人 |
| 目的 | 受付→整理→出力までを実際に流す |
| 入力 | サンプル入力（本文・ファイル） |
| 出力 | 案件記録・下書き・集計 CSV |
| 次の操作 | /works・/contact |
| 見せ場 | 通常処理と AI と未接続を別々に表示する |
| 減らす手間 | 転記・集計・下書き |
| 画像 | 工程図と実出力 |
| 保存 | なし（画面内） |
| 副作用・権限 | scenario_started ほかサンプル funnel |
| 完了判定と証拠 | flow.spec.ts / qa:flow（3 トラック 7 工程 / axe） |

状態: 19/23 該当。N/A — **通信失敗**（サーバーへ出ない）、**offline**（サーバーへ出ない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）

### `/history`

TSUDOWA / static / 実体 1 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 実際に何が作られたか確かめたい人 |
| 目的 | 年ごとの制作事実を、コミット参照つきで読む |
| 入力 | なし |
| 出力 | なし |
| 次の操作 | 各記録の関連ページ |
| 見せ場 | 手動更新であることと、確認日を明記した一覧 |
| 減らす手間 | 「本当に動いているのか」を聞く手間 |
| 画像 | なし（文字のみ） |
| 保存 | なし |
| 副作用・権限 | なし |
| 完了判定と証拠 | hq.spec.ts（件数と日付形式）/ unit hq.test.ts |

状態: 4/23 該当。N/A — **入力中**（入力欄がない）、**選択**（選択状態を持つ操作がない）、**loading**（サーバー取得なし（静的生成））、**進行**（複数段階の処理がない）、**成功**（完了する操作がない）、**空**（内容は静的で、空になりえない）、**検索0件**（検索がない）、**validation error**（入力がない）、**通信失敗**（リクエストを出さない）、**offline**（初回表示後は追加取得がない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）、**競合**（他者と共有する状態がない）、**部分成功**（分割される処理がない）、**retry**（失敗しうる処理がない）、**cancel**（取り消す操作がない）、**undo**（変更が残らない）、**disabled**（無効化する操作がない）、**再訪**（保存しないため初回と同じ）

### `/lab`

TSUKUTTA LAB / static / 実体 1 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | プロダクト・実験に関心がある人 |
| 目的 | LAB が独立した活動であることを知る |
| 入力 | なし |
| 出力 | なし |
| 次の操作 | /・/contact |
| 見せ場 | 公開 URL が未確認であることを、書かずに済ませない |
| 減らす手間 | 存在しないリンクを踏む手間 |
| 画像 | LAB のコンセプト面 |
| 保存 | なし |
| 副作用・権限 | なし |
| 完了判定と証拠 | hq.spec.ts（tsukuttalab へのリンクが 0 件） |

状態: 4/23 該当。N/A — **入力中**（入力欄がない）、**選択**（選択状態を持つ操作がない）、**loading**（サーバー取得なし（静的生成））、**進行**（複数段階の処理がない）、**成功**（完了する操作がない）、**空**（内容は静的で、空になりえない）、**検索0件**（検索がない）、**validation error**（入力がない）、**通信失敗**（リクエストを出さない）、**offline**（初回表示後は追加取得がない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）、**競合**（他者と共有する状態がない）、**部分成功**（分割される処理がない）、**retry**（失敗しうる処理がない）、**cancel**（取り消す操作がない）、**undo**（変更が残らない）、**disabled**（無効化する操作がない）、**再訪**（保存しないため初回と同じ）

### `/partners`

TETSU WORKS / form / 実体 1 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 制作会社・デザイナー（実装の外注先を探す側） |
| 目的 | 実装品質を確かめ、条件を整理して相談する |
| 入力 | 相談文メーカー |
| 出力 | 下書き |
| 次の操作 | /kissa・/forme・各デモ・/contact |
| 見せ場 | 実装の証拠を screenshot ではなく動く店に置く |
| 減らす手間 | スキル確認のやりとり |
| 画像 | 各デモの実画面 |
| 保存 | なし |
| 副作用・権限 | brief_created |
| 完了判定と証拠 | link-qa / visual-qa 4 幅 |

状態: 20/23 該当。N/A — **権限不足**（アカウントの概念がない）、**競合**（同じ相談を二人が編集しない）、**undo**（送信前は編集、送信後は取り消せないと明記）

### `/privacy`

TSUDOWA / static / 実体 1 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 問い合わせ前に取り扱いを確認したい人 |
| 目的 | 収集する項目・保管・第三者提供を確認する |
| 入力 | なし |
| 出力 | なし |
| 次の操作 | /contact |
| 見せ場 | 実際に送信される項目だけを書く |
| 減らす手間 | 個別の確認メール |
| 画像 | なし |
| 保存 | なし |
| 副作用・権限 | なし |
| 完了判定と証拠 | link-qa / visual-qa 4 幅 |

状態: 4/23 該当。N/A — **入力中**（入力欄がない）、**選択**（選択状態を持つ操作がない）、**loading**（サーバー取得なし（静的生成））、**進行**（複数段階の処理がない）、**成功**（完了する操作がない）、**空**（内容は静的で、空になりえない）、**検索0件**（検索がない）、**validation error**（入力がない）、**通信失敗**（リクエストを出さない）、**offline**（初回表示後は追加取得がない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）、**競合**（他者と共有する状態がない）、**部分成功**（分割される処理がない）、**retry**（失敗しうる処理がない）、**cancel**（取り消す操作がない）、**undo**（変更が残らない）、**disabled**（無効化する操作がない）、**再訪**（保存しないため初回と同じ）

### `/projects/[slug]`

TETSU WORKS / static / 実体 11 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 特定の制作例を詳しく見たい人 |
| 目的 | 背景・課題・実装・納品物・QA・限界を読む |
| 入力 | なし |
| 出力 | なし |
| 次の操作 | /demos/<slug>・操作できる版・関連サービス・/contact |
| 見せ場 | 限界を価格の隣に書く。操作できる版はヒーロー直後 |
| 減らす手間 | 実績の有無を確認する往復 |
| 画像 | 3 幅プレビュー + ビジュアルストーリー |
| 保存 | なし |
| 副作用・権限 | portfolio_project_open / portfolio_price_view |
| 完了判定と証拠 | sales-hub.spec.ts（全 11 件を axe つきで巡回） |

状態: 4/23 該当。N/A — **入力中**（入力欄がない）、**選択**（選択状態を持つ操作がない）、**loading**（サーバー取得なし（静的生成））、**進行**（複数段階の処理がない）、**成功**（完了する操作がない）、**空**（内容は静的で、空になりえない）、**検索0件**（検索がない）、**validation error**（入力がない）、**通信失敗**（リクエストを出さない）、**offline**（初回表示後は追加取得がない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）、**競合**（他者と共有する状態がない）、**部分成功**（分割される処理がない）、**retry**（失敗しうる処理がない）、**cancel**（取り消す操作がない）、**undo**（変更が残らない）、**disabled**（無効化する操作がない）、**再訪**（保存しないため初回と同じ）

実体: `/projects/admin` / `/projects/automation` / `/projects/booking` / `/projects/cafe` / `/projects/creative` / `/projects/csv` / `/projects/ec` / `/projects/improvement` / `/projects/inbox` / `/projects/qa` / `/projects/saas`

### `/report`

TETSU WORKS / form / 実体 1 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 毎週・毎月、同じ形式のCSVを集計している人 |
| 目的 | 列の意味を一度決めて保存し、二回目からは質問なしで集計する |
| 入力 | CSVファイル（手元のもの、またはサンプル3週分）、列の役割、通貨、重複条件、読み取れない行の修正 |
| 出力 | 合計と件数、前回との差、変更の記録、集計結果CSV、例外だけのCSV、報告文用の要約 |
| 次の操作 | /report/recipes・/report/history |
| 見せ場 | 二回目に何も聞かないこと。聞くのは前になかった列と税区分のときだけ |
| 減らす手間 | 列の対応をやり直す／重複を目視で探す／集計し直す／前回と見比べる／読み取れない行を表計算で直す |
| 画像 | なし（数字と差分が主役） |
| 保存 | report sandbox（ルール・履歴・作業中の下書き）+ 破損時の控え |
| 副作用・権限 | なし。ファイルはサーバーへ送信しない |
| 完了判定と証拠 | report-flow.test.ts（30 件・三週フロー）/ report.spec.ts / qa:visual |

状態: 20/23 該当。N/A — **権限不足**（アカウントの概念がない）、**競合**（同じ相談を二人が編集しない）、**undo**（送信前は編集、送信後は取り消せないと明記）

### `/rescue`

TETSU WORKS / form / 実体 1 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 何を頼めばよいか分からない人 |
| 目的 | 困りごとから相談文を組み立てる |
| 入力 | 困りごとの選択と自由記述 |
| 出力 | 下書き |
| 次の操作 | /services・各デモ・/kissa・/contact |
| 見せ場 | 専門用語を使わずに始められる |
| 減らす手間 | 要件が固まる前に相談できない停滞 |
| 画像 | なし |
| 保存 | なし |
| 副作用・権限 | brief_created |
| 完了判定と証拠 | link-qa / visual-qa 4 幅 |

状態: 20/23 該当。N/A — **権限不足**（アカウントの概念がない）、**競合**（同じ相談を二人が編集しない）、**undo**（送信前は編集、送信後は取り消せないと明記）

### `/services`

TETSU WORKS / catalogue / 実体 1 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 小さく頼める範囲を探している人 |
| 目的 | 範囲・価格・期間が決まったメニューを選ぶ |
| 入力 | なし |
| 出力 | なし |
| 次の操作 | /services/<slug>・/rescue |
| 見せ場 | 公開しているメニューだけを並べる |
| 減らす手間 | 「いくらから頼めるか」の往復 |
| 画像 | 各メニューのデモ画面 |
| 保存 | なし |
| 副作用・権限 | service_view |
| 完了判定と証拠 | figma-sales.spec.ts / link-qa |

状態: 13/23 該当。N/A — **loading**（一覧は静的生成で、絞り込みは同期処理）、**進行**（複数段階の処理がない）、**通信失敗**（リクエストを出さない）、**offline**（初回表示後は追加取得がない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）、**競合**（他者と共有する状態がない）、**部分成功**（分割される処理がない）、**retry**（失敗しうる処理がない）、**undo**（絞り込みは解除で戻る）

### `/services/[slug]`

TETSU WORKS / form / 実体 2 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | そのメニューを頼むか決める人 |
| 目的 | 範囲・進め方・検証済みの内容を読み、相談文を作る |
| 入力 | 相談文メーカーの選択と自由記述 |
| 出力 | 下書き（コピー / フォームへ引き継ぎ） |
| 次の操作 | /contact（内容を引き継ぐ） |
| 見せ場 | 検証した内容と、測っていない数字を分けて書く |
| 減らす手間 | 要件整理と最初の一通 |
| 画像 | 対応デモの実画面 |
| 保存 | なし（下書きは画面内） |
| 副作用・権限 | brief_created / consultation_cta_clicked |
| 完了判定と証拠 | offers のユニットテスト / e2e の下書き検査 |

状態: 20/23 該当。N/A — **権限不足**（アカウントの概念がない）、**競合**（同じ相談を二人が編集しない）、**undo**（送信前は編集、送信後は取り消せないと明記）

実体: `/services/csv-routine` / `/services/web-fix`

### `/works`

TETSU WORKS / catalogue / 実体 1 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 依頼を検討している人 |
| 目的 | 自分の仕事に近い制作例と参考価格を見つける |
| 入力 | カテゴリ絞り込み（12 分類） |
| 出力 | 絞り込まれたカード一覧、カテゴリのパーマリンク |
| 次の操作 | /projects/*・/demos/*・/kissa・/forme・/contact |
| 見せ場 | 操作できる 2 店を冒頭で名指しする |
| 減らす手間 | 「何ができるか」を一件ずつ聞く手間 |
| 画像 | 11 件 × desktop / mobile プレビュー |
| 保存 | なし（絞り込みは URL とメモリ） |
| 副作用・権限 | portfolio_visit / portfolio_price_view / 操作できる版クリック |
| 完了判定と証拠 | works-connect.spec.ts / figma-sales.spec.ts / qa:images |

状態: 13/23 該当。N/A — **loading**（一覧は静的生成で、絞り込みは同期処理）、**進行**（複数段階の処理がない）、**通信失敗**（リクエストを出さない）、**offline**（初回表示後は追加取得がない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）、**競合**（他者と共有する状態がない）、**部分成功**（分割される処理がない）、**retry**（失敗しうる処理がない）、**undo**（絞り込みは解除で戻る）

### `/works/[category]`

TETSU WORKS / catalogue / 実体 12 件 / sitemap 掲載

| 項目 | 内容 |
| --- | --- |
| 利用者 | 依頼の種類が決まっている人 |
| 目的 | その分類の制作例・範囲・必要な素材・参考価格を読む |
| 入力 | なし（URL が分類） |
| 出力 | なし |
| 次の操作 | /projects/*・/contact（分類を引き継ぐ） |
| 見せ場 | 分類ごとの対応範囲と、事前に必要な素材 |
| 減らす手間 | 見積もり前のやりとり |
| 画像 | その分類のカード群 + カテゴリ KV |
| 保存 | なし |
| 副作用・権限 | portfolio_category_view |
| 完了判定と証拠 | sales-hub.spec.ts（12 分類の所属を完全一致で検査） |

状態: 13/23 該当。N/A — **loading**（一覧は静的生成で、絞り込みは同期処理）、**進行**（複数段階の処理がない）、**通信失敗**（リクエストを出さない）、**offline**（初回表示後は追加取得がない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）、**競合**（他者と共有する状態がない）、**部分成功**（分割される処理がない）、**retry**（失敗しうる処理がない）、**undo**（絞り込みは解除で戻る）

実体: `/works/api` / `/works/apps` / `/works/automation` / `/works/coding` / `/works/design` / `/works/ec` / `/works/improvement` / `/works/lp` / `/works/nextjs` / `/works/qa` / `/works/responsive` / `/works/web`

### `/experience/[slug]`

TETSU WORKS / demo / 実体 11 件

| 項目 | 内容 |
| --- | --- |
| 利用者 | デモだけを画面いっぱいで見たい人 |
| 目的 | ポートフォリオの枠なしで同じデモを操作する |
| 入力 | デモと同じ |
| 出力 | デモと同じ |
| 次の操作 | 操作できる版・/projects/<slug> |
| 見せ場 | 枠を外しても、架空であることの表示は外さない |
| 減らす手間 | 共有時に説明を足す手間 |
| 画像 | デモと同じ |
| 保存 | デモと同じ（同じ sandbox を共有） |
| 副作用・権限 | なし |
| 完了判定と証拠 | standalone.spec.ts（11 件すべて） |

状態: 19/23 該当。N/A — **通信失敗**（サーバーへ出ない）、**offline**（サーバーへ出ない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）

実体: `/experience/admin` / `/experience/automation` / `/experience/booking` / `/experience/cafe` / `/experience/creative` / `/experience/csv` / `/experience/ec` / `/experience/improvement` / `/experience/inbox` / `/experience/qa` / `/experience/saas`

### `/report/recipes`

TETSU WORKS / catalogue / 実体 1 件

| 項目 | 内容 |
| --- | --- |
| 利用者 | 保存したルールを確かめたい人 |
| 目的 | 列の対応・通貨・重複条件・承認済みの税区分と版を読む |
| 入力 | 削除の確認 |
| 出力 | 削除、書き出し／読み込み |
| 次の操作 | /report |
| 見せ場 | 版を上げても前の版を消さない（過去の集計が説明できなくなるため） |
| 減らす手間 | 「前回どう決めたか」を思い出す手間 |
| 画像 | なし |
| 保存 | report sandbox |
| 副作用・権限 | なし |
| 完了判定と証拠 | report.spec.ts / report-flow.test.ts |

状態: 13/23 該当。N/A — **loading**（一覧は静的生成で、絞り込みは同期処理）、**進行**（複数段階の処理がない）、**通信失敗**（リクエストを出さない）、**offline**（初回表示後は追加取得がない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）、**競合**（他者と共有する状態がない）、**部分成功**（分割される処理がない）、**retry**（失敗しうる処理がない）、**undo**（絞り込みは解除で戻る）

### `/report/history`

TETSU WORKS / catalogue / 実体 1 件

| 項目 | 内容 |
| --- | --- |
| 利用者 | 先週の数字を確かめたい人 |
| 目的 | 過去の処理の合計・件数・例外の数と、使ったルールの版を読む |
| 入力 | なし |
| 出力 | 書き出し／読み込み |
| 次の操作 | /report |
| 見せ場 | 合計と読み込み行数を分けて記録する。明細は保存しない |
| 減らす手間 | 前回の報告を探し直す手間 |
| 画像 | なし |
| 保存 | report sandbox（最大 40 件） |
| 副作用・権限 | なし |
| 完了判定と証拠 | report.spec.ts / report-flow.test.ts |

状態: 13/23 該当。N/A — **loading**（一覧は静的生成で、絞り込みは同期処理）、**進行**（複数段階の処理がない）、**通信失敗**（リクエストを出さない）、**offline**（初回表示後は追加取得がない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）、**競合**（他者と共有する状態がない）、**部分成功**（分割される処理がない）、**retry**（失敗しうる処理がない）、**undo**（絞り込みは解除で戻る）

### `/daybook`

TETSU WORKS / form / 実体 1 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | 予約の希望を文章で送りたい人 |
| 目的 | 希望の時間が取れるか判定し、取れないときは実際に空いている候補まで出す |
| 入力 | 依頼の文章（例文4通りあり）、名前、連絡先、候補の選択、合意 |
| 出力 | 読み取れた日時と読み取れなかった項目、空き候補、仮押さえ、確定、変更の差分、依頼ごとの記録 |
| 次の操作 | /daybook/admin |
| 見せ場 | 断って終わらせないこと。埋まっているときに、前後の準備時間まで踏まえた候補が出る |
| 減らす手間 | 空き時間を聞き直す往復／営業時間を調べる／仮押さえの期限を覚えておく／変更のたびに全部を確認し直す |
| 画像 | なし（時刻と状態が主役） |
| 保存 | daybook sandbox（相談・仮押さえ・予約）+ 破損時の控え |
| 副作用・権限 | なし。実際の予約は入らず、メールも送らない |
| 完了判定と証拠 | daybook-flow.test.ts（16 件）/ daybook.spec.ts / qa:visual |

状態: 20/23 該当。N/A — **権限不足**（アカウントの概念がない）、**競合**（同じ相談を二人が編集しない）、**undo**（送信前は編集、送信後は取り消せないと明記）

### `/daybook/admin`

TETSU WORKS / console / 実体 1 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | 予約を受ける側 |
| 目的 | 承認が要るものと、承認すると何が変わるかを読んで決める |
| 入力 | 承認、取消 |
| 出力 | 予約者の合意とお店の承認の2つの記録、変更の旧→新、前後の準備時間を含む台帳、他システムの予定 |
| 次の操作 | /daybook |
| 見せ場 | 片方の記録だけでは確定しないこと。承認を押しても、断られた理由が記録に残るだけ |
| 減らす手間 | 何を承認するのか探す／変更で何が変わるのか読み直す／自分の予定と他システムの予定を見分ける |
| 画像 | なし |
| 保存 | daybook sandbox（お客さま側と同じ1つの記録） |
| 副作用・権限 | なし。自分が作っていない予定は変更も削除もしない |
| 完了判定と証拠 | daybook-flow.test.ts / daybook.spec.ts |

状態: 20/23 該当。N/A — **通信失敗**（サーバーへ出ない）、**offline**（サーバーへ出ない）、**権限不足**（運営画面を分けているが、認証は持たない（明記））

### `/kissa`

KISSA（架空店舗） / shop / 実体 1 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | 店を探している人（の役） |
| 目的 | 店の雰囲気を見て、注文か予約へ進む |
| 入力 | なし |
| 出力 | なし |
| 次の操作 | /kissa/menu・/kissa/reserve |
| 見せ場 | 架空であることを全ページのバナーで明示 |
| 減らす手間 | —（入口） |
| 画像 | 店舗写真 5 点 |
| 保存 | kissa sandbox（読み取りのみ） |
| 副作用・権限 | なし。noindex, follow |
| 完了判定と証拠 | kissa.spec.ts / qa:kissa 56 項目 / qa:performance |

状態: 20/23 該当。N/A — **通信失敗**（サーバーへ出ない（支払いは内部シミュレーター））、**offline**（サーバーへ出ないため、切断しても操作は続く）、**権限不足**（ログインのない架空店舗）

### `/kissa/menu`

KISSA（架空店舗） / catalogue / 実体 1 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | 注文する人 |
| 目的 | 14 品から選び、カートに入れる |
| 入力 | カテゴリ切替・商品選択 |
| 出力 | カート（商品・数量） |
| 次の操作 | /kissa/menu/<id>・/kissa/order |
| 見せ場 | 売り切れが運営画面と連動する |
| 減らす手間 | 店頭での確認 |
| 画像 | 14 品それぞれのイラスト（coverage 14/14） |
| 保存 | kissa sandbox |
| 副作用・権限 | なし |
| 完了判定と証拠 | kissa.spec.ts / qa:images |

状態: 13/23 該当。N/A — **loading**（一覧は静的生成で、絞り込みは同期処理）、**進行**（複数段階の処理がない）、**通信失敗**（リクエストを出さない）、**offline**（初回表示後は追加取得がない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）、**競合**（他者と共有する状態がない）、**部分成功**（分割される処理がない）、**retry**（失敗しうる処理がない）、**undo**（絞り込みは解除で戻る）

### `/kissa/reserve`

KISSA（架空店舗） / shop / 実体 1 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | 席を取る人 |
| 目的 | 空席から選び、仮押さえして確定する |
| 入力 | 日時・人数・席 |
| 出力 | 予約番号、予約記録 |
| 次の操作 | /kissa/my（変更・取消） |
| 見せ場 | 仮押さえの期限切れが自動で解放される |
| 減らす手間 | 電話・往復メール |
| 画像 | 席の配置図 |
| 保存 | kissa sandbox |
| 副作用・権限 | なし |
| 完了判定と証拠 | shop-reservation のユニットテスト / kissa.spec.ts |

状態: 20/23 該当。N/A — **通信失敗**（サーバーへ出ない（支払いは内部シミュレーター））、**offline**（サーバーへ出ないため、切断しても操作は続く）、**権限不足**（ログインのない架空店舗）

### `/kissa/order`

KISSA（架空店舗） / shop / 実体 1 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | 注文を確定する人 |
| 目的 | 受取時間を決めて注文する |
| 入力 | 受取時間・支払方法 |
| 出力 | 注文番号、注文記録 |
| 次の操作 | /kissa/my |
| 見せ場 | 調理時間と営業時間から受取枠を出す（7 日先まで繰り上げ） |
| 減らす手間 | 電話での時間調整 |
| 画像 | カート内の商品イラスト |
| 保存 | kissa sandbox |
| 副作用・権限 | なし（支払いは内部シミュレーター） |
| 完了判定と証拠 | shop-cart-order のユニットテスト / kissa.spec.ts |

状態: 20/23 該当。N/A — **通信失敗**（サーバーへ出ない（支払いは内部シミュレーター））、**offline**（サーバーへ出ないため、切断しても操作は続く）、**権限不足**（ログインのない架空店舗）

### `/kissa/my`

KISSA（架空店舗） / shop / 実体 1 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | 注文・予約した人 |
| 目的 | 状況を見て、変更・取消し、データを持ち出す |
| 入力 | 変更内容、読み込むファイル |
| 出力 | 更新された記録、書き出した JSON |
| 次の操作 | /kissa/menu・/kissa/reserve |
| 見せ場 | 自分の予約は自分の枠を塞がない（変更できる） |
| 減らす手間 | 問い合わせての変更 |
| 画像 | なし |
| 保存 | kissa sandbox + 破損時の控え（--backup） |
| 副作用・権限 | なし |
| 完了判定と証拠 | sandbox.spec.ts（書出し・読込・控え・隔離）/ kissa.spec.ts |

状態: 20/23 該当。N/A — **通信失敗**（サーバーへ出ない（支払いは内部シミュレーター））、**offline**（サーバーへ出ないため、切断しても操作は続く）、**権限不足**（ログインのない架空店舗）

### `/kissa/admin`

KISSA（架空店舗） / console / 実体 1 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | 店側（の役） |
| 目的 | 注文を進め、売り切れを切り替え、売上を見る |
| 入力 | 状態変更・販売状態の切替 |
| 出力 | 注文状態、在庫表示、集計 |
| 次の操作 | — |
| 見せ場 | 客側と同じ記録を店側から動かす |
| 減らす手間 | 二重台帳 |
| 画像 | なし |
| 保存 | kissa sandbox |
| 副作用・権限 | なし。認証はないと明記 |
| 完了判定と証拠 | kissa.spec.ts / qa:controls |

状態: 20/23 該当。N/A — **通信失敗**（サーバーへ出ない）、**offline**（サーバーへ出ない）、**権限不足**（運営画面を分けているが、認証は持たない（明記））

### `/kissa/menu/[productId]`

KISSA（架空店舗） / shop / 実体 14 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | その商品を選ぶ人 |
| 目的 | 温度・サイズを選び、数量を決めてカートに入れる |
| 入力 | バリエーション・数量 |
| 出力 | カート行 |
| 次の操作 | /kissa/order・/kissa/menu |
| 見せ場 | 売り切れの組み合わせは押せず、理由を書く |
| 減らす手間 | 店頭での質問 |
| 画像 | 商品イラスト（詳細サイズ） |
| 保存 | kissa sandbox |
| 副作用・権限 | なし |
| 完了判定と証拠 | kissa.spec.ts（14 実体を巡回） |

状態: 20/23 該当。N/A — **通信失敗**（サーバーへ出ない（支払いは内部シミュレーター））、**offline**（サーバーへ出ないため、切断しても操作は続く）、**権限不足**（ログインのない架空店舗）

実体: `/kissa/menu/cheesecake` / `/kissa/menu/cold-brew` / `/kissa/menu/curry` / `/kissa/menu/drip-house` / `/kissa/menu/espresso` / `/kissa/menu/herbal-tea` / `/kissa/menu/hojicha-latte` / `/kissa/menu/latte` / `/kissa/menu/pudding` / `/kissa/menu/sandwich` / `/kissa/menu/seasonal-citrus` / `/kissa/menu/seasonal-mont-blanc` / `/kissa/menu/sencha` / `/kissa/menu/toast-set`

### `/forme`

FORME（架空店舗） / shop / 実体 1 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | 商品を探している人（の役） |
| 目的 | 店の世界観を見て、商品一覧へ進む |
| 入力 | なし |
| 出力 | なし |
| 次の操作 | /forme/items |
| 見せ場 | 架空であることを全ページのバナーで明示 |
| 減らす手間 | —（入口） |
| 画像 | 商品写真 3 点 + テクスチャ |
| 保存 | forme sandbox（読み取りのみ） |
| 副作用・権限 | なし。noindex, follow |
| 完了判定と証拠 | forme.spec.ts / qa:forme 62 項目 |

状態: 20/23 該当。N/A — **通信失敗**（サーバーへ出ない（支払いは内部シミュレーター））、**offline**（サーバーへ出ないため、切断しても操作は続く）、**権限不足**（ログインのない架空店舗）

### `/forme/items`

FORME（架空店舗） / catalogue / 実体 1 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | 商品を比べる人 |
| 目的 | 絞り込み・並び替え・3 点比較で候補を絞る |
| 入力 | 絞り込み・並び替え・比較選択・お気に入り |
| 出力 | 比較表、お気に入り |
| 次の操作 | /forme/items/<id> |
| 見せ場 | 在庫が数として出る（売り切れを隠さない） |
| 減らす手間 | 問い合わせての在庫確認 |
| 画像 | 6 商品 × 色（写真 3 / 作図 8） |
| 保存 | forme sandbox |
| 副作用・権限 | なし |
| 完了判定と証拠 | forme.spec.ts / qa:images（forme.colour.media） |

状態: 13/23 該当。N/A — **loading**（一覧は静的生成で、絞り込みは同期処理）、**進行**（複数段階の処理がない）、**通信失敗**（リクエストを出さない）、**offline**（初回表示後は追加取得がない）、**権限不足**（アカウントの概念がない）、**期限切れ**（期限を持つデータがない）、**競合**（他者と共有する状態がない）、**部分成功**（分割される処理がない）、**retry**（失敗しうる処理がない）、**undo**（絞り込みは解除で戻る）

### `/forme/cart`

FORME（架空店舗） / shop / 実体 1 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | 注文する人 |
| 目的 | 受け取り方法と地域を決め、送料込みで注文する |
| 入力 | 配送/店頭、地域、支払方法 |
| 出力 | 注文番号、注文記録、在庫の減少 |
| 次の操作 | /forme/my |
| 見せ場 | 住所を取らずに送料を出す（地域だけ） |
| 減らす手間 | 送料の問い合わせ |
| 画像 | カート内の商品画像 |
| 保存 | forme sandbox |
| 副作用・権限 | なし（支払いは内部シミュレーター、再実行は冪等） |
| 完了判定と証拠 | forme-shop のユニットテスト / forme.spec.ts |

状態: 20/23 該当。N/A — **通信失敗**（サーバーへ出ない（支払いは内部シミュレーター））、**offline**（サーバーへ出ないため、切断しても操作は続く）、**権限不足**（ログインのない架空店舗）

### `/forme/my`

FORME（架空店舗） / shop / 実体 1 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | 注文した人 |
| 目的 | 状況を見て、支払い・取消しをし、データを持ち出す |
| 入力 | 支払い、取消し、読み込むファイル |
| 出力 | 更新された注文、戻る在庫、書き出した JSON |
| 次の操作 | /forme/items |
| 見せ場 | タブを閉じたあとでも支払える（決済が注文カードにある） |
| 減らす手間 | 支払い方法の問い合わせ |
| 画像 | 注文商品の画像 |
| 保存 | forme sandbox + 破損時の控え |
| 副作用・権限 | なし |
| 完了判定と証拠 | sandbox.spec.ts / forme.spec.ts |

状態: 20/23 該当。N/A — **通信失敗**（サーバーへ出ない（支払いは内部シミュレーター））、**offline**（サーバーへ出ないため、切断しても操作は続く）、**権限不足**（ログインのない架空店舗）

### `/forme/admin`

FORME（架空店舗） / console / 実体 1 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | 店側（の役） |
| 目的 | 在庫を動かし、取り扱いを止め、発送し、売上を見る |
| 入力 | 在庫の増減、公開停止、発送 |
| 出力 | 在庫、注文状態、集計 |
| 次の操作 | — |
| 見せ場 | 未払いのまま発送できない |
| 減らす手間 | 二重台帳 |
| 画像 | なし |
| 保存 | forme sandbox（在庫は公開カタログからの差分） |
| 副作用・権限 | なし。認証はないと明記 |
| 完了判定と証拠 | forme.spec.ts / qa:controls |

状態: 20/23 該当。N/A — **通信失敗**（サーバーへ出ない）、**offline**（サーバーへ出ない）、**権限不足**（運営画面を分けているが、認証は持たない（明記））

### `/forme/items/[productId]`

FORME（架空店舗） / shop / 実体 6 件 / **noindex**

| 項目 | 内容 |
| --- | --- |
| 利用者 | その商品を選ぶ人 |
| 目的 | 色とサイズを選び、価格と在庫を確かめて入れる |
| 入力 | 色・サイズ・数量 |
| 出力 | カート行 |
| 次の操作 | /forme/cart |
| 見せ場 | 色を変えると写真・価格・在庫が同時に変わる |
| 減らす手間 | 組み合わせごとの問い合わせ |
| 画像 | 色ごとの写真、なければ作図 |
| 保存 | forme sandbox |
| 副作用・権限 | なし |
| 完了判定と証拠 | forme.spec.ts（6 実体）/ forme-shop のユニットテスト |

状態: 20/23 該当。N/A — **通信失敗**（サーバーへ出ない（支払いは内部シミュレーター））、**offline**（サーバーへ出ないため、切断しても操作は続く）、**権限不足**（ログインのない架空店舗）

実体: `/forme/items/bottle` / `/forme/items/care-kit` / `/forme/items/lunch-box` / `/forme/items/mug` / `/forme/items/tote` / `/forme/items/tumbler`

